/** @type {import('next').NextConfig} */
const nextConfig = {
  // three/examples/jsm ships as ESM; transpiling keeps Next happy across versions.
  transpilePackages: ['three'],
};

export default nextConfig;
